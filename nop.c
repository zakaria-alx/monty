#include "monty.h"

/**
  *fn_nop- nothing
  *@head: stack's head
  *@line_number: line_number
  *Return: no return
 */
void fn_nop(stack_t **head, unsigned int line_number)
{
	(void) line_number;
	(void) head;
}
